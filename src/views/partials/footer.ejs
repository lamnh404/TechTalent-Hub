
<footer class="bg-dark text-white text-center py-4 mt-5">
    <div class="container">
        <p class="mb-0">Copyright © 2025 TechTalent Hub. All rights reserved.</p>
        <div class="mt-2">
            <a href="/about" class="text-white mx-2">About</a>
            <a href="/contact" class="text-white mx-2">Contact</a>
            <a href="/privacy" class="text-white mx-2">Privacy Policy</a>
            <a href="/terms" class="text-white mx-2">Terms of Service</a>
        </div>
    </div>
</footer>

<!-- jQuery (required for Bootstrap JavaScript) -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<!-- Popper.js (required for Bootstrap dropdowns) -->
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
<!-- Bootstrap JS -->
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

<script>
    document.addEventListener("DOMContentLoaded", function() {
        // Chỉ chạy nếu đang ở trang Dashboard
        if (document.querySelector('.dashboard-page')) {
            const header = document.querySelector('.dashboard-header');
            const sidebar = document.querySelector('.sidebar');
            
            // Tạo nút hamburger
            const toggleBtn = document.createElement('button');
            toggleBtn.className = 'btn btn-light mr-3 d-lg-none shadow-sm';
            toggleBtn.innerHTML = '<i class="fas fa-bars"></i>';
            toggleBtn.onclick = function() {
                sidebar.classList.toggle('show');
            };

            // Chèn nút vào đầu header
            if (header) {
                header.insertBefore(toggleBtn, header.firstChild);
            }

            // Click ra ngoài để đóng sidebar
            document.addEventListener('click', function(event) {
                const isClickInside = sidebar.contains(event.target) || toggleBtn.contains(event.target);
                if (!isClickInside && sidebar.classList.contains('show')) {
                    sidebar.classList.remove('show');
                }
            });
        }
    });
</script>

</body>
</html>
